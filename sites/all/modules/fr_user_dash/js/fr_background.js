/*(function($) {
	$(document).ready(function() {
	
		var iFrequency = 1000; // expressed in miliseconds
		var myInterval = 0;

		startLoop();
		
		function doSomething(time){
		console.log(time);
		////// dont enable
			/*var important = $('.most-important').map(function() {
			var test = $(this).html();
			var d=new Date();
			var tours = new Date();
			var tour = Date.prase($(this).html());
			var current_time = d.getTime();
			var current = dat.prase(current_time);
			var diff = (tour - current);
			console.log(diff);
 				return $(this).html();
 			}).get();
 			//alert(important);
 		////	
  }

// STARTS and Resets the loop if any
function startLoop() {
   if(myInterval > 0) clearInterval(myInterval);  // stop
   var d=new Date();
   var t=d.toLocaleTimeString();
    myInterval = setInterval(function(){doSomething(t)}, iFrequency );  // run
    console.log('sent');
   }

	});
})(jQuery);*/