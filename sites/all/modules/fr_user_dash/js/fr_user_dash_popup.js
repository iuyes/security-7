(function($){

 /* 'use strict';
  //
  // Global variable.
  // Helps us determine if the current modal is being queued for display.
  //
  var modalQueued = false;

  //
  // Bind the live 'click' event to all anchor elemnets with the data-reveal-id attribute.
  //
  $(document).on('click', 'a[data-help-id]', function ( event ) {
    //
    // Prevent default action of the event.
    //
    event.preventDefault();
    //
    // Get the clicked anchor data-reveal-id attribute value.
    //
    var modalLocation = $( this ).attr( 'data-help-id' );
    //
    // Find the element with that modalLocation id and call the reveal plugin.
    //
    //$( '#' + modalLocation ).reveal( $( this ).data() );
    var modal = $( '#' + modalLocation );

    var element_position = modal.top;
    
    $( '#' + modalLocation ).addClass('help-modal-active');
    modal.addClass('help-modal-active');
    modal.css('top', '200px !important');
    modal.show();
    
    
   
  });*/
})(jQuery);